import { LightningElement } from "lwc";

export default class PortalSample extends LightningElement {
  // Minimal sample for future Experience Cloud integration
  connectedCallback() {
    // Placeholder for future portal logic
  }
}
